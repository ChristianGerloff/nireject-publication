# nireject-publication / assets

This folder contains python and Matlab assets.
The assets are included as submodules using gitmodules or downloaded via the bash script during the docker build process.